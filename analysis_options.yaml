include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "**/build/**"
    - "**/*.g.dart"
    - "**/*.freezed.dart"

# The user wants to be STRICT with lint rules. Do not relax any rules here. Fix the code instead.

