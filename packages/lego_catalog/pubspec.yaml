name: lego_catalog
description: Generic LEGO catalog client with pluggable backends.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

executables:
  lego_catalog: lego_catalog

dependencies:
  args: ^2.7.0
  dio: ^5.9.0
  dio_smart_retry: ^7.0.1
  json_annotation: ^4.12.0
  logging: ^1.3.0

dev_dependencies:
  build_runner: ^2.6.0
  json_serializable: ^6.11.1
  lints: ^6.0.0
  test: ^1.26.3
