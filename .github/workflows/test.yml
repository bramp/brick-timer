# Flutter Test Workflow Template
name: Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Check generated app artwork mtimes
        run: make check-app-artwork-mtime
        continue-on-error: true

      - name: Check formatting
        run: make format && git diff --exit-code
        continue-on-error: true

      - name: Static analysis
        run: make analyze
        continue-on-error: true

      - name: Run tests
        run: make test-ci
